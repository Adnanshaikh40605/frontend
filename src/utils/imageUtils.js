// Helper functions for working with images

/**
 * Simple function to return the image URL without any optimization
 * @param {string} src - The image source URL
 * @returns {string} The original image URL
 */
export const getImageUrl = (src) => {
  return src;
};
